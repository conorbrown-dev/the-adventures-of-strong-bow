# Kindergarten curriculum review packet

The self-contained human-review artifacts are:

- `kindergarten-review-packet.html` — printable, filterable generated examples and review evidence.
- `kindergarten-review-packet.json` — the machine-readable companion.

Regenerate them with `npm run curriculum:content:review-packet`.
