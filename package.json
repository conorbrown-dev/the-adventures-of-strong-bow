{
  "name": "molly-learning-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "serve -s dist -l $PORT",
    "typecheck": "tsc --noEmit",
    "start": "serve -s dist -l $PORT"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "serve": "^14.2.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
